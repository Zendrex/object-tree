---
"@zendrex/object-tree": minor
---

Export `renderValue`, `resolveOptions`, and `ResolvedOptions` from the package entry point to allow low-level rendering without constructing an `ObjectTree` instance.
